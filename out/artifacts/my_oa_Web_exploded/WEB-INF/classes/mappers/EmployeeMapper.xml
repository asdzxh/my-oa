<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.zxh.mapper.EmployeeMapper">
    <select id="selectById" parameterType="Long" resultType="top.zxh.entity.Employee">
        SELECT *
        FROM adm_employee
        WHERE employee_id = #{value}
    </select>
</mapper>